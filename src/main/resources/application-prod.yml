# Keep the connection alive if idle for a long time (needed in production)
spring:
  datasource:
    testWhileIdle: true
    validationQuery: SELECT 1
# JPA
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
# Upload
  http:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
# Thymeleaf
  thymeleaf:
    cache: false

bonecp:
  driverClass: com.mysql.jdbc.Driver
  url: jdbc:mysql://51.254.131.44:8082/banana?useUnicode=yes&characterEncoding=UTF-8
  username: banana
  password: bananaspring
  idleMaxAgeInMinutes: 240
  idleConnectionTestPeriodInMinutes: 60
  maxConnectionsPerPartition: 10
  minConnectionsPerPartition: 1
  partitionCount: 2
  acquireIncrement: 5
  statementsCacheSize: 100

# Upload parameters
upload:
  path: /bitnami/tomcat/data/upload-dir

# Mail
mail:
  protocol: smtp
  host: SSL0.OVH.NET
  port: 587
  smtp:
    auth: false
    starttls:
      enable: true
  from: me@localhost
  username: cyril.phamle@magelbe.com
  password: joesong1805